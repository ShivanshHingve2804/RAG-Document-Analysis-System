Drop your `demo.gif` (or `demo.mp4`) here — recorded from the Streamlit app
showing: upload PDF → build index → ask a question → grounded answer with
sources. Tools like `LICEcap`, `ScreenToGif` (Windows), `Kap` (Mac), or
`peek` (Linux) all export directly to GIF.
